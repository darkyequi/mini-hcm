{
  "name": "mini-hcm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "firebase-admin": "^14.1.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
